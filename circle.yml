# For automated test

deployment:
  dockerhub:
    branch: dev
    commands:
      - $DOCKER_HUB_POCTESTWEB_TRIGGER
    branch: master
    commands:
      - $DOCKER_HUB_POCTESTWEB_TRIGGER

test:
  override:    
    - mocha