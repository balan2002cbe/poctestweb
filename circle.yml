# For automated test

deployment:
  dockerhub:
    branch: dev
    commands:
      - $DOCKER_HUB_WEB_TRIGGER	
    branch: master
    commands:
      - $DOCKER_HUB_WEB_TRIGGER

test:
  override:    
    - mocha